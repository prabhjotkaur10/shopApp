<%= form_for(@shopdetail) do |f| %>
  <% if @shopdetail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shopdetail.errors.count, "error") %> prohibited this shopdetail from being saved:</h2>

      <ul>
      <% @shopdetail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
